{
  "name": "mock-api",
  "version": "1.0.0",
  "description": "test-api",
  "main": "index.js",
  "scripts": {
    "dev": "json-server-auth db.json -r routes.json",
    "dev:generate": "node index.js && json-server-auth db.json -r routes.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@faker-js/faker": "^8.4.1",
    "json-server": "^0.17.4",
    "json-server-auth": "^2.1.0"
  }
}
